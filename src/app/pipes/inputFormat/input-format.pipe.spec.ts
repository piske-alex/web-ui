import { InputFormatPipe } from './input-format.pipe';

describe('InputFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new InputFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
