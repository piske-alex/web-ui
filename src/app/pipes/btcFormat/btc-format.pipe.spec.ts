import { BtcFormatPipe } from './btc-format.pipe';

describe('BtcFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BtcFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
