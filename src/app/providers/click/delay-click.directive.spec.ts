import { DelayClickDirective } from './delay-click.directive';

describe('DelayClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayClickDirective();
    expect(directive).toBeTruthy();
  });
});
